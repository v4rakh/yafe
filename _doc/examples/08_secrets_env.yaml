# Secrets from environment variables
# Demonstrates: secrets with from.env
runs-on: host
secrets:
  - name: api_key
    from:
      env: TEST_API_KEY

steps:
  - name: api-call
    cmd: |
      echo "Using API key: ${{ secrets.api_key }}"
    kind: shell
