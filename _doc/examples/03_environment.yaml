# Environment variables
# Demonstrates: env field with KEY=value format
runs-on: host
steps:
  - cmd: |
      echo "DEBUG=$DEBUG"
      echo "APP_NAME=$APP_NAME"
      echo "VERSION=$VERSION"
    kind: shell
    env:
      - DEBUG=true
      - APP_NAME=myapp
      - VERSION=1.0.0
