# Secrets from files
# Demonstrates: secrets with from.file
runs-on: host
secrets:
  - name: db_password
    from:
      file: /tmp/yafe-test-secret.txt

steps:
  - name: db-connect
    cmd: |
      echo "Connecting with password: ${{ secrets.db_password }}"
    kind: shell
